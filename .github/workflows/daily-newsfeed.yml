# Daily Newsfeed Generator
#
# Currently runs locally because the data source (CSV from Hex) is a
# manual export and the DB is too large for git.
#
# Local usage:
#   python -m vllm_issue_tracker.cli generate-newsfeed
#   python -m vllm_issue_tracker.cli generate-newsfeed --date 2026-04-05 --days 3
#
# TODO: Automate once data source is available in CI (e.g. S3, Fivetran API,
# or GitHub Actions artifact). Uncomment the workflow below when ready.
#
# name: Daily Newsfeed
#
# on:
#   schedule:
#     # 11:59 PM PT = 06:59 UTC next day
#     - cron: "59 6 * * *"
#   workflow_dispatch:
#
# jobs:
#   generate:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#       - run: pip install -e .
#       - run: python -m vllm_issue_tracker.cli generate-newsfeed
#         env:
#           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
#       - run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add dashboard/index.html build/newsfeed/
#           git diff --staged --quiet || git commit -m "newsfeed: $(TZ=America/Los_Angeles date +%Y-%m-%d)" && git push
